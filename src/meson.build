yasl_sources = ['yasl.c']
yasllib = shared_library('yasl',
                         yasl_sources,
                         version : meson.project_version(),
                         include_directories : inc,
                         install : true)
